{
  "name": "typecek-example",
  "private": true,
  "type": "module",
  "scripts": {
    "compile": "node ../packages/cli/dist/bin.js compile",
    "render": "tsx src/render.ts",
    "build": "pnpm compile && pnpm render",
    "clean": "node ../packages/cli/dist/bin.js clean && rm -rf dist out"
  },
  "dependencies": {
    "@typecek/cli": "workspace:*",
    "@typecek/runtime": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.19.0"
  },
  "version": "0.1.0"
}
