{
  "name": "typecek-example",
  "private": true,
  "type": "module",
  "scripts": {
    "compile": "typecek compile",
    "render": "tsx src/render.ts",
    "build": "pnpm compile && pnpm render",
    "clean": "typecek clean && rm -rf dist out"
  },
  "dependencies": {
    "@typecek/cli": "workspace:*",
    "@typecek/runtime": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.19.0"
  }
}
